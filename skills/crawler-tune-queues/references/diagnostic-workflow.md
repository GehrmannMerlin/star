# `crawler-tune-queues` 诊断流程与分类规则

> 本文件定义本 Skill 的诊断流程、问题分类规则与证据卡映射。判断依据与边界以主决策日志与已批准设计规格为准。

## 诊断流程

1. 读取项目上下文、目标、约束与已确认问题证据。处理 Stellaris 问题时，先读取 `stellaris-crawler-context` 生成的项目上下文包路径。
2. 确定诊断聚焦点（任务队列 / 并发 / 背压 / 任务级重试 / 优先级·公平性 / 缓存 / 暂停恢复 / 资源控制），聚焦该点，不做无边界全栈诊断。
3. 检查相关代码、配置、依赖版本、日志、指标、Trace、资源数据、压测结果，定位具体证据位置。
4. 引用匹配版本的证据卡（`queue-performance.md`）比对项目行为与规范/实现/工程证据。
5. 分类：代码缺陷 / 配置错误 / 版本兼容 / 设计不足 / 外部条件 / 未知项。
6. 证据不足时只提出最小补充取证或受控基准测试/故障注入；根因未证实前不提出代码修改。
7. 输出可追溯问题证据、根因或待验证假设、影响范围、修复方向、性能基线和回归建议。
8. 根因和方向获批后，将选定方向交给 `crawler-writing-plans-bridge`。

## 问题分类规则

- **代码缺陷**：队列/并发代码未按规范/实现证据处理（如任务重复入队、并发控制错误、背压未生效）。
- **配置错误**：队列/性能配置与固定版本行为不符（如并发数、队列容量、超时设置错误）。
- **版本兼容**：依赖版本与 API 行为不匹配（如 celery/reactive-streams 版本差异）。
- **设计不足**：缺乏背压、优先级、公平调度、资源控制等设计层面缺失。
- **外部条件**：目标站点拒绝、限流、资源环境受限等——只能给合规降级/等待/终止结论，不得伪装成已修复（主决策日志 §12/§54）。
- **未知项**：证据不足或冲突，保持候选假设，显式标记，不判定为确定根因。

每条结论必须明确区分：事实、证据、综合推断、待确认项。综合推断必须显式标记为"推断"，不得伪装成资料中的确定结论（主决策日志 §14）。

置信度使用固定枚举：高（证据充分且指向唯一分类）、中（证据充分但多个分类可能）、低（证据不足或冲突，仅候选假设）、不确定（无法判断）。

## 跨任务问题重点

- 特别识别任务丢失/重复、重试风暴、队列饥饿、背压失效等跨任务问题。
- 每个优化方向必须给出可比较性能基线方法与回归门槛（压测/指标对比），确保优化效果可验证。
- 禁止用默认值或猜测掩盖跨任务问题；必须暴露真实问题，供用户批准修复方向。

## 证据卡映射

共享证据卡：`docs/superpowers/knowledge/evidence-cards/queue-performance.md`；知识视图：`docs/superpowers/knowledge/skill-views/crawler-tune-queues.md`。

| 诊断聚焦点 | 证据卡 | 固定资料路径 |
|---|---|---|
| 任务队列与调度 | TQ-QUEUE-001、TQ-QUEUE-002、TQ-QUEUE-003 | celery `celery/app`、`celery/worker`；scrapy `core/scheduler.py`、`core/engine.py` |
| 并发与背压 | TQ-BACK-001、TQ-BACK-002、TQ-BACK-003 | reactive-streams `api`；celery `celery/worker`；scrapy `core/engine.py` |
| 优先级与公平性 | TQ-PRIO-001、TQ-PRIO-002 | scrapy `core/scheduler.py`；celery `celery/app/amqp.py` |
| 缓存与暂停恢复 | TQ-CACHE-001、TQ-CACHE-002、TQ-CACHE-003 | celery `celery/backends`、`celery/app/control.py`、`celery/worker/control.py`、`celery/worker/state.py` |
| 性能基线与故障注入 | TQ-PERF-001、TQ-PERF-002 | locust `locust/`；celery `celery/app/autoretry.py` |

每条依据必须可追溯：从诊断记录追到固定版本资料、证据卡、上下文包条目或决策日志节号。只给仓库首页、模糊文件夹或没有版本的引用不满足要求。

## 受控基准测试与故障注入边界

- 诊断确有必要时，可以指导 Agent 执行受控基准测试和故障注入用于验证假设。
- 测试不得接管项目的生产任务队列；不得执行无效压测或过度故障注入；诊断复现限受控、合规。
- 测试必须使用已保存证据或最小化受控环境，不得发起新的对外抓取或绕过访问控制。

## 证据不足与冲突

- 证据不足时不得把结论判定为确定根因；保留候选状态并标记未知项。
- 现场证据、项目上下文、已批准要求冲突时并列报告，不静默覆盖任一来源。
- 未解决的声明标记为不确定（非确定性），不得伪装成确定结论。
- 固定版本证据卡与既有诊断记录不得被静默改写；更新只通过新增修订进行。
- 外部条件（站点拒绝/资源环境受限）只能给合规降级/等待/终止结论，不得伪装成已修复。
- 跨任务问题必须暴露，不得用默认值或猜测掩盖。
- 不得因新技术更新默认要求升级；是否升级以证据和项目约束为准。
