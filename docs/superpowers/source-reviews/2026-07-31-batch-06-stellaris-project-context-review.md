# 爬虫知识资料库首遍资料审查——第 6 批：Stellaris 项目专用层

> 审查日期：2026-07-31
>
> 对应一级方向：Stellaris 项目专用层
>
> 对应 Skill：`stellaris-crawler-context`
>
> 状态：等待用户核对
>
> 边界：本文只审查哪些项目内材料能够向 Agent 提供可信的 Stellaris 上下文，以及哪些材料必须降级或排除。未下载或克隆任何第三方源码，未联网搜索通用 Agent、RAG 或项目记忆框架，未创建或实施 Skill，未进入 `writing-plans`，也未修改爬虫代码。

## 1. 审查口径与当前项目事实

### 1.1 已确认职责边界

- `stellaris-crawler-context` 每次处理 Stellaris 问题时采用“动态核验＋已批准记录”，向 `crawler-triage-incidents` 输出项目上下文包。
- 上下文包只包含当前代码能够证明的事实、用户已经批准的要求、历史设计草案、冲突与未知项，以及建议分诊重点；它不诊断根因、不提出修复方案。
- 当前事实、批准记录、历史草案和快照发生冲突时，必须并列报告，不能由 Agent 静默选择。
- 项目专用层只保存 Stellaris 上下文和调用规则，不复制其他 18 个 Skill 的通用知识，也不建设独立项目索引服务或记忆系统。

### 1.2 现场核验事实

- `E:\Stellaris` 当前不是 Git 仓库；根目录只有 `.superpowers` 和 `docs`。
- 根目录不存在 `AGENTS.md`、`README.md`、`package.json`、pnpm/npm/yarn 锁文件、`tsconfig.json`、Dockerfile、Compose 文件、爬虫源码、测试或可运行程序。
- `docs` 当前只有 8 个 Markdown 文件，合计约 0.2 MiB：两份 brainstorming 决策日志、一份历史设计规格和五份已经完成核对的首遍审查报告。
- `.superpowers/brainstorm` 当前约 284.5 MiB、7,275 个文件，包含可视化/表格检查工具的 `node_modules`、临时服务器信息和会话辅助产物；它们不是 Stellaris 爬虫源码、项目依赖或已批准需求。

### 1.3 本批不联网扩展的理由

- 外部仓库无法证明本地项目当前有哪些文件、版本、配置、测试或运行状态，也无法证明用户批准过哪些 Stellaris 要求。
- 通用故障分诊、架构评审、资料治理和重点技术栈知识已由前五批覆盖；在项目专用层重复收集 Agent memory、RAG、代码索引或项目模板仓库会破坏最小充分边界。
- 因此本批的合格来源是项目内一手材料和现场只读核验结果；建议准入的第三方在线资料与外部源码仓库均为零。

## 2. Agent 建议：准入的项目内权威资料（未确认）

| 来源 | 权威层级 | 当前状态 | 可证明内容 | 使用边界 |
|---|---|---|---|---|
| 当前工作区的现场只读核验 | 当前事实的最高优先来源 | 可用；目前没有爬虫实现制品 | 目录结构、源码、依赖清单、锁文件、配置、测试、启动/验证命令、工具版本和运行状态是否真实存在 | 必须每次重新核验；“不存在”只在核验时点成立，不得永久缓存 |
| `docs/superpowers/brainstorming/2026-07-31-crawler-knowledge-skills-decision-log.md` | 本轮 Skills 与资料库的已批准决策权威记录 | 活跃维护；当前至第 71 节 | 用户已确认的 Skill 定位、职责、权限、资料准入、版本、安全、容量、E 盘和阶段边界 | 只把明确记录为已确认的内容视为要求；状态描述或旧事实若与现场证据冲突，必须报告冲突 |
| `docs/superpowers/brainstorming/2026-07-30-official-biography-crawler-decision-log.md` | 前序爬虫设计决策记录 | 文档自身标注已整体批准并暂停 `writing-plans` | 产品目标、业务准入规则、历史架构与验收决议 | 按主决策日志第 6 节，仅作为背景、历史决策和可重新审视的设计输入；不得覆盖 2026-07-31 主日志 |
| `docs/superpowers/specs/2026-07-30-official-biography-crawler-design.md` | 历史设计规格 | 文档自身标注已整体批准并暂停实施计划 | 旧版完整系统设计、技术栈基线、数据流、业务规则和验收指标 | 不是当前资料筛选或 Skill 修复的硬约束；不得把设计中的预期文件、版本或组件写成当前项目事实 |
| `docs/superpowers/source-reviews/2026-07-31-batch-01-source-governance-review.md` 至 `batch-05-core-stack-review.md` | 已批准资料分类的详细佐证 | 五批均已由用户核对，并在主日志第 67 至 71 节记录 | 每批在线资料、准入/候选/拒绝仓库、许可证、版本和体积判断的详细来源 | 是否已批准以及批准范围以主决策日志为准；报告正文不能自行升级为新的项目要求 |
| 将来真实出现的项目实现制品 | 未来当前事实来源 | 目前缺失 | 源码、清单、锁文件、配置、迁移、测试、容器定义、运行手册、启动与验证命令 | 出现后按现场内容读取，不依据历史设计推测路径、工具或版本；生成物和第三方依赖目录必须与手写项目制品分开 |
| 用户后续明确批准并写入主决策日志的要求 | 未来批准要求来源 | 按会话逐项产生 | 新增、修改或撤销的项目约束 | 只有先持久化到主决策日志并核验后，才进入可跨会话复用的批准要求层 |

### 已发现的状态冲突

- 主决策日志第 1 节仍写着前序设计规格“待用户整体批准”；两份 2026-07-30 文档自身现均标注“用户已整体批准，暂停 `writing-plans`”。
- 该冲突不改变主决策日志第 6 节已经确认的权限边界：前序规格仍然只是可重新审视的历史设计输入，不是当前硬约束。
- `stellaris-crawler-context` 应报告这一状态差异，而不能借任一文档状态字段把历史设计变成当前实现事实。

## 3. Agent 建议：外部资料与源码准入清单（未确认）

本批建议准入的第三方在线资料为 **0 项**，建议进入第二遍固定 Commit/浅克隆资格范围的外部仓库为 **0 个**。

理由如下：

- Stellaris 当前事实只能由项目现场制品证明；
- 用户批准状态只能由项目内决策日志证明；
- 历史设计输入已经存在于项目内，无需从外部重新采集；
- 通用方法与技术知识已经由前五批的资料和源码清单覆盖；
- 不为项目专用层引入与现有 18 个 Skill 重复的通用上下文框架。

本批因此不增加第三方资料库体积，也不触发第二遍下载任务。

## 4. Agent 建议：候选来源清单（未确认，不作为当前事实）

| 候选来源 | 当前状态 | 可能用途 | 暂不准入原因 | 升格条件 |
|---|---|---|---|---|
| 项目上下文快照或文件索引 | 尚未创建 | 加速定位目录、配置和关键文件 | 快照会过期，不能替代现场核验；当前项目文件极少，没有创建价值 | 项目实现制品出现且规模足以证明索引必要；仍须标注生成时间并现场复核 |
| 故障现场日志、Trace、截图、数据库只读查询和测试输出 | 尚无具体故障证据 | 支持一次具体分诊 | 只对特定时间、环境和故障有效，不能预先当作项目基线 | 用户提供或授权采集具体证据，并记录来源、时间、环境和完整性信息 |
| 将来的 Git Commit、分支、Tag、Issue、PR 与 CI 记录 | 当前项目不是 Git 仓库 | 证明代码演变、回归来源和评审结果 | 目前不存在可核验的项目仓库，也不能假定未来托管位置 | 项目建立真实版本库后，按具体任务和 Commit 定向读取 |
| 尚未批准的 brainstorming 建议或审查报告 | 当前第 6 批报告即属此类 | 为下一项决策提供候选输入 | 未经用户确认，不得进入“已批准要求” | 用户批准后，先更新主决策日志并核验 |
| 本机工具与运行环境快照 | 可通过只读命令临时获得 | 区分项目问题与本机 Node、Docker、WSL 等环境问题 | 会随时间变化；前序报告中的版本只代表审查时点 | 每次相关问题现场重查，并明确“环境事实”而非“项目锁定版本” |

## 5. Agent 建议：拒绝来源清单（未确认）

| 拒绝来源 | 当前事实 | 拒绝理由 |
|---|---|---|
| `.superpowers/brainstorm/**` 的工具依赖与临时状态 | 约 284.5 MiB、7,275 个文件，含 `node_modules`、服务端口/令牌和辅助产物 | 属 brainstorming 工具工作区，不是 Stellaris 爬虫源码、依赖或需求；纳入会造成严重项目事实污染 |
| 个人 Skills 目录中的安装副本 | 位于项目目录之外，且主日志已规定安装副本不是唯一可信源码 | 不能证明 Stellaris 当前代码或批准要求，也不得绕过项目内未来 `skills/` 源码进行直接编辑 |
| 未写入主决策日志的跨会话记忆或聊天概括 | 不具备项目内持久化与可复核位置 | 可能遗漏、压缩或混淆已确认决策、当前事实和 Agent 建议 |
| 通用 Agent memory、RAG、代码索引和项目上下文框架仓库 | 本批未搜索、未下载 | 会把项目专用层扩张为新的运行组件，并与资料治理、分诊和技术栈 Skill 重复；不能证明 Stellaris 事实 |
| `.superpowers` 中安装的第三方包及其许可证/README | 是辅助工具的传递依赖，不是项目清单声明的依赖 | 不得因物理上位于 `E:\Stellaris` 就误判为项目技术栈或可修复代码 |
| 把历史设计文档当作当前代码事实或不可修改硬约束的读取方式 | 两份 2026-07-30 文档只描述设计，不存在对应实现制品 | 违反主决策日志第 6、39、41 节；会掩盖“项目尚未实现”以及设计与现场之间的冲突 |

## 6. 事实、建议与后续边界

### 当前项目事实

- 当前项目没有爬虫实现、版本库、清单、锁文件、配置、测试或运行命令。
- `.superpowers` 中的大量依赖属于辅助工具，不属于 Stellaris 项目实现。
- 现有可持久化项目材料只有 `docs` 下的 8 个 Markdown 文件。

### 历史设计输入

- 两份 2026-07-30 文档继续作为历史设计和业务背景，可被后续证据质疑或修改，不得覆盖主决策日志或冒充当前实现。

### Agent 建议，等待用户核对

- 建议批准第 2 节的项目内来源权威顺序、第 3 节“零外部资料/零外部仓库准入”、第 4 节候选来源和第 5 节拒绝来源。
- 本批获批后，六个一级方向的第一遍分类才算全部完成；批准本身仍不触发克隆、Skill 创建或项目代码修改。
- 下一步应先汇总六批获准清单、去重并核算第二遍容量与版本绑定阻塞项，再单独向用户提交是否开始第二遍的关键决策；不得自动下载。
